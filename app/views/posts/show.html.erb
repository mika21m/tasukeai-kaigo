<div class="jumbotron">
  <div class="row">
    <div class="card-columns">
      <div class="card">
        <div class="card-img-top text-center">
          <%= image_tag @post.image.variant(resize: '250x250') if @post.image.attached? %>
        </div>
        <div class="card-body">
          <% if user_signed_in? && current_user.id == @post.user_id %>
            <p class="card-title"><%= link_to @post.user.name, user_path(current_user.id) %></p>
          <% else %>
            <p class="card-title"><%= @post.user.name %></p>
          <% end %>
            <p class="card-text"><%= @post.text %></p>
            <div class="comment-form">
              <% if user_signed_in? %>
                <%= form_with model: [@post, @comment], id: "comment-form" do |f| %>
                  <%= f.text_area :text , class: "comment-text"%>
                  <%= f.submit "コメントをする", class: "comment-submit" %>
                <% end %>
              <% end %>
            </div>
            <div id="comments">
                <% @post.comments.each do |comment| %>
                  <div class="comment">
                    <p class="user-info"><%= comment.user.name %>： </p>
                    <p><%= comment.text%></p>
                  </div>
                <% end %>
            </div>
        </div>
      </div>
    </div>
  </div>
</div>